pipeline {
    agent any
        stages {
        stage('test') {
            steps {      
                sh "ls -la"
            }
        }
        stage('deploy') {
            steps {  
                sh "id"
                sh "whoami"
                sh "echo deploy"
            }
        }
    }
}
